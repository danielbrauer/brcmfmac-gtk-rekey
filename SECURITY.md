# Security and privacy

This repository must remain free of credentials and private network details.

Do not submit Wi-Fi credentials, network names, access-point or client hardware
addresses, private IP addresses, hostnames, authentication tokens, or raw logs
that contain them. Reduce reports to event ordering, elapsed time, key index,
cipher, return code, and sanitized device/software versions.

The instrumentation must never emit key bytes or network identifiers.
